services:
  jairo-oficina:
    build: .
    restart: unless-stopped
    expose:
      - "8080"
    volumes:
      - jairo_oficina_data:/app/data
    environment:
      - PORT=8080
      - DATA_DIR=/app/data
      - SECRET_KEY=troque-essa-chave-depois

volumes:
  jairo_oficina_data:
